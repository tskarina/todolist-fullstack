import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Importe o componente principal App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
